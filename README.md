TRAIT- trait defines the reusability of code and functions that can be use in many files.

Namespace- namespaces allows the user to use same function name, class name, or trait having different namespace.
